@model IEnumerable<eAuto.Models.EstadoAuto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEstado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEstadoAuto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEstadoAuto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEstadoAuto })
        </td>
    </tr>
}

</table>
